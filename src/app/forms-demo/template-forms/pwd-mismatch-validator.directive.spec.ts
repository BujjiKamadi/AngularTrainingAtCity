import { PwdMismatchValidatorDirective } from './pwd-mismatch-validator.directive';

describe('PwdMismatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdMismatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
