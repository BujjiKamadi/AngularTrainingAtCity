import { PwdPatternValidatorDirective } from './pwd-pattern-validator.directive';

describe('PwdPatternValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdPatternValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
